# Database Configuration (Docker)
DATABASE_URL=postgresql://postgres:root@postgres:5432/project_tracker

# Session Secret
SESSION_SECRET=pinnacle-ai-super-secret-key-change-in-production

# Server Configuration
PORT=7855
NODE_ENV=production

# Daily member update + Teams report settings
DAILY_REPORT_TIMEZONE=Asia/Kolkata
DAILY_REPORT_SEND_TIME=22:00
TEAMS_WEBHOOK_URL=

# Default Admin Configuration (used to bootstrap/repair the default admin account)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_EMAIL=admin@pinnacle.ai
DEFAULT_ADMIN_NAME=Girish Desai

# Email Configuration (for password reset)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=adminpinnacleai@gmail.com
EMAIL_PASS=peaptyyqxnyetoha
